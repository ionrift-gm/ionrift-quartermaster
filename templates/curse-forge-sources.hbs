<form class="ionrift-loot-pool-config ionrift-curse-forge-sources">
    {{#if alreadyCompiled}}
    <p class="pool-instructions">
        {{localize 'IONRIFT.QUARTERMASTER.CONFIG.CurseForgeAlreadyCompiled' recipeCount=recipeCount}}
    </p>
    {{else}}
    <div class="scroll-forge-welcome">
        <div class="scroll-forge-welcome-icon">
            <i class="fas fa-skull"></i>
        </div>
        <h3>{{localize 'IONRIFT.QUARTERMASTER.CONFIG.CurseForgeWelcomeTitle'}}</h3>
        <p class="scroll-forge-welcome-desc">
            {{localize 'IONRIFT.QUARTERMASTER.CONFIG.CurseForgeWelcomeDesc' recipeCount=recipeCount}}
        </p>
        <p class="scroll-forge-welcome-action">
            {{localize 'IONRIFT.QUARTERMASTER.CONFIG.CurseForgeWelcomeAction'}}
        </p>
        <p class="scroll-forge-welcome-note">
            {{localize 'IONRIFT.QUARTERMASTER.CONFIG.CurseForgeWelcomeNote'}}
        </p>
    </div>
    {{/if}}

    <div class="scroll-forge-toolbar">
        <button type="button" class="cache-btn-accent curse-forge-select-all">
            <i class="fas fa-check-double"></i> {{localize 'IONRIFT.QUARTERMASTER.CONFIG.SelectAll'}}
        </button>
        <button type="button" class="cache-btn-accent curse-forge-select-none">
            <i class="fas fa-times"></i> {{localize 'IONRIFT.QUARTERMASTER.CONFIG.SelectNone'}}
        </button>
    </div>

    {{#each groups}}
    <div class="pool-group">
        <div class="pool-group-header">
            <i class="fas fa-box-archive"></i> {{this.label}}
        </div>
        {{#each this.packs}}
        <label class="pool-pack-row">
            <input type="checkbox" name="pack-{{this.id}}" {{#if this.enabled}}checked{{/if}} />
            <span class="pool-pack-label">{{this.label}}</span>
            <span class="pool-pack-id">{{this.id}} {{localize 'IONRIFT.QUARTERMASTER.CONFIG.ItemsCount' count=this.count}}</span>
        </label>
        {{/each}}
    </div>
    {{/each}}

    <footer class="pool-footer">
        <button type="submit" class="cache-btn-accent">
            <i class="fas fa-hammer"></i> {{localize 'IONRIFT.QUARTERMASTER.CONFIG.Compile'}}
        </button>
    </footer>
</form>
